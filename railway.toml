[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.leviathan"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
