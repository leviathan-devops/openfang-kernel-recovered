[build]
builder = "nixpacks"
dockerfilePath = "Dockerfile.leviathan"

[deploy]
# Temporarily disabled healthcheck to debug startup crash
# healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
